#!/usr/bin/env sh
set -eu
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
if [ "${SKIP_GIT_HOOKS:-}" = "1" ]; then
  exit 0
fi
if ! command -v bun >/dev/null 2>&1; then
  echo "pre-commit: bun not on PATH; install Bun or set SKIP_GIT_HOOKS=1" >&2
  exit 1
fi
bun run check
